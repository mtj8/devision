services:
  pgdb:
    image: postgres:16
    container_name: pgdb
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    env_file:
      - .env    
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data    # allows db to be persistent in dev build
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 3s
      timeout: 3s
      retries: 30

  django:
    build:
      context: .   # builds from /backend
      dockerfile: Dockerfile
    container_name: django
    working_dir: /app # where the commands will be run
    command: bash -lc "python manage.py runserver 0.0.0.0:8000" #python manage.py migrate &&
    environment:
      DATABASE_URL: ${DATABASE_URL} 
    env_file:
      - .env
    volumes:
      - ./django-project:/app # mount the Django app folder
    ports:
      - "8000:8000"
    depends_on:
      pgdb:
        condition: service_healthy

volumes:
  pgdata:
